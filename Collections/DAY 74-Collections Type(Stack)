PROBLEM-Collections Type(Stack)

SOLUTION-
package Collections;
// Java program to demonstrate the
// working of a stack
import java.util.*;
public class StackCollection {



		// Main Method
		public static void main(String args[])
		{
			Stack<String> stack = new Stack<String>();
			stack.push("Java");
			stack.push("Collections");
			stack.push("Type");
			stack.push("Stack");

			// Iterator for the stack
			Iterator<String> itr = stack.iterator();

			// Printing the stack
			while (itr.hasNext()) {
				System.out.print(itr.next() + " ");
			}

			System.out.println();

			stack.pop();

			// Iterator for the stack
			itr = stack.iterator();

			// Printing the stack
			while (itr.hasNext()) {
				System.out.print(itr.next() + " ");
			}
		}
	}


